<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.itranz.fyp">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.ITranz"
        tools:targetApi="31">

        <!-- Entry Activity -->
        <activity
            android:name="com.itranz.fyp.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ITranz">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name="com.itranz.fyp.activities.NICApprovalActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.FareApprovalActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.QRScanActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.PaymentActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.SeatReservationActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.QRPaymentActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.StripePaymentActivity" android:exported="true"/>
        <activity android:name="com.itranz.fyp.activities.TopUpWalletActivity" android:exported="true"/>
        <!-- WalletTopUpActivity commented out for now -->

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name="com.itranz.fyp.notifications.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>
